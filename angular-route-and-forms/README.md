# angular-route-and-forms

To run, use `node server.js` in the `node-server` directory. Then, run `ng serve` in the `angular-app` directory. Make sure that all the requirements (Angular, Node, etc.) are installed.